package com.cambiosmart.demo.spring.boot.operaciones;

public enum TipoOperacion {
    COMPRA_USD,  // El cliente compra USD (envía PEN, recibe USD)
    VENTA_USD    // El cliente vende USD (envía USD, recibe PEN)
}
